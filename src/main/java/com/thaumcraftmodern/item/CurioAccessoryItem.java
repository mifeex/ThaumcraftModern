package com.thaumcraftmodern.item;

import net.minecraft.world.item.Item;
import top.theillusivec4.curios.api.type.capability.ICurioItem;

/** A tag-assigned Curios accessory with no effect of its own. */
public class CurioAccessoryItem extends Item implements ICurioItem {
    public CurioAccessoryItem(Properties properties) {
        super(properties);
    }
}
